[package]
name = "sync-proto"
version = "1.0.0"
edition = "2021"
description = "Glyphio sync wire protocol v1: shared record + request/response types for any client or server implementation."
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
